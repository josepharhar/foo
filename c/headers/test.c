#include <stdio.h>
#include <string.h>

void testPrint(char *message) {
    /*char *fullMessage = strcat("testPrint: ", message);
    fullMessage = strcat(fullMessage, "\n");
    printf("%s", fullMessage);*/
    printf("%s\n", message);
    printf("TEST_MESSAGE\n");
}
